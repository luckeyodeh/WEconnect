language: node_js
dist: trusty
node_js:
- "9"
install:
  - npm install
env:
  - NODE_ENV=test
cache:
  directories:
    - "node_modules"
services:
  - postgresql
before_script:
   - psql -c 'create database test;' -U postgres
script:
  - npm test
after_success :
    - npm run coverage

